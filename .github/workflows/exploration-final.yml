name: Final player exploration verification
on:
  push:
    branches: [fix/player-exploration-20260922]
    paths: [.github/workflows/exploration-final.yml]
permissions:
  contents: read
jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: write
    outputs:
      revision: ${{ steps.commit.outputs.revision }}
    steps:
      - uses: actions/checkout@v4
      - name: Apply the checksummed source-only delta
        run: |
          echo '77c044951683d4c249f159dfe9d31f21cc64def515c1376f474ffed5fc53a807  qa/transfer/final-warmup.patch' | sha256sum -c -
          git apply --check qa/transfer/final-warmup.patch
          git apply qa/transfer/final-warmup.patch
          rm qa/transfer/final-warmup.patch
      - id: commit
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/game scripts tests docs qa/transfer
          git commit -m "Warm actual covered portal pixels before input and explain hydraulic experiment outcomes"
          git push origin HEAD:fix/player-exploration-20260922
          echo "revision=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"
  native:
    needs: apply
    runs-on: ubuntu-latest
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        room: [24, 28, 30]
    env:
      BUILD_COMMIT: ${{ needs.apply.outputs.revision }}
      ROOM: ${{ matrix.room }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.apply.outputs.revision }}
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: |
          npm ci
          npm install --no-save --package-lock=false puppeteer-core@24.16.0
          npm run build
          node scripts/stamp-build.mjs v39-campaign-30
      - name: Record ordinary routes and separately marked interior inspection views
        run: |
          npm run preview -- --host 127.0.0.1 --port 4173 >exploration-preview.log 2>&1 &
          for i in {1..30}; do curl -fsS http://127.0.0.1:4173/ >/dev/null && break; sleep 1; done
          export CHROME_PATH="$(command -v google-chrome || command -v chromium)"
          node scripts/open-browser-review.mjs
      - name: Encode actual captured motion
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq ffmpeg
          ffmpeg -y -framerate 15 -i "qa/open-browser/${ROOM}-motion/%05d.png" -c:v libx264 -crf 20 -pix_fmt yuv420p -movflags +faststart "qa/open-browser/${ROOM}-motion.mp4"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: exploration-final-native-${{ matrix.room }}
          path: |
            qa/open-browser/*.png
            qa/open-browser/*.json
            qa/open-browser/*.mp4
            exploration-preview.log
            dist/build-info.json
          retention-days: 14
  profile:
    needs: apply
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: cb882f9b3e6dc200dedd3ab05a4b3fb0fa9f7faa
          path: before
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.apply.outputs.revision }}
          path: after
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Compare identical measurement script and staged poses on one runner
        run: |
          cp after/scripts/profile-exploration.mjs before/scripts/profile-exploration.mjs
          export CHROME_PATH="$(command -v google-chrome || command -v chromium)"
          ROOT="$PWD"
          mkdir -p "$ROOT/profile-evidence"
          for VERSION in before after; do
            cd "$ROOT/$VERSION"
            npm ci
            npm install --no-save --package-lock=false puppeteer-core@24.16.0
            npm run build
            BUILD_COMMIT="$(git rev-parse HEAD)" node scripts/stamp-build.mjs v39-campaign-30
            if [ "$VERSION" = before ]; then PORT=4174; else PORT=4175; fi
            npm run preview -- --host 127.0.0.1 --port "$PORT" >"$ROOT/profile-evidence/$VERSION-preview.log" 2>&1 &
            for i in {1..30}; do curl -fsS "http://127.0.0.1:$PORT/" >/dev/null && break; sleep 1; done
            PAGE_URL="http://127.0.0.1:$PORT/" OUT="$ROOT/profile-evidence/$VERSION" node scripts/profile-exploration.mjs
            cp dist/build-info.json "$ROOT/profile-evidence/$VERSION/build-info.json"
          done
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: exploration-final-portal-profile
          path: profile-evidence/
          retention-days: 14
