diff --git a/docs/passes/P09B_PORTAL_PIPELINE.md b/docs/passes/P09B_PORTAL_PIPELINE.md
index 7cc3d55..b040af5 100644
--- a/docs/passes/P09B_PORTAL_PIPELINE.md
+++ b/docs/passes/P09B_PORTAL_PIPELINE.md
@@ -28,3 +28,5 @@ The initial complete CPU audit also highlights room10 and contact-heavy mechanis
 The first full-suite candidate failed nine existing scene-workload contracts: hidden compile-only portal nodes remained attached after loading. The retained fix detaches them in `finally` after compilation, rather than weakening those existing tests or excluding them from counts. It adds a dedicated lifecycle regression. Full-suite status is rechecked on this corrected tree.
 
 The extended replace-during-transit regression exposed a candidate-only lifetime hazard: pooling the scene root could resurrect an old camera exit-plane reference when that slot was reused. The retained implementation pools GPU meshes/materials but creates a fresh lightweight logical root per placement; the old frame root stays detached. The regression fails on the first pooled version and passes after the fix. This is not presented as a pre-existing released-game bug.
+
+A secondary-render-pass audit added a conservative bypass for transmissive materials and scene override materials. Refraction may sample full-view background outside the aperture, so those scenes must not use visibility-based cropping. All21 current campaign scenes and imported assets were scanned: no active transmissive mesh was found (an unused material factory is not a rendered mesh). The fallback protects future/material-review configurations without lowering glass quality. A regression verifies off-aperture background remains submitted.
diff --git a/src/game/LabPortalRenderCulling.js b/src/game/LabPortalRenderCulling.js
index ca308c8..32d14e1 100644
--- a/src/game/LabPortalRenderCulling.js
+++ b/src/game/LabPortalRenderCulling.js
@@ -25,7 +25,7 @@ export class LabPortalRenderCulling {
   begin(scene, camera, rect, width, height, reversedDepth = false) {
     if (this.active) throw new Error('Portal culling is already active');
     this.tested = 0;
-    if (!this.enabled) return;
+    if (!this.enabled || scene.overrideMaterial) return;
     const { x, y, width: w, height: h } = rect;
     if (![x, y, w, h, width, height].every(Number.isFinite)
       || w <= 0 || h <= 0 || width <= 0 || height <= 0) return;
@@ -44,7 +44,15 @@ export class LabPortalRenderCulling {
     // World matrices are refreshed by the caller with the same policy as
     // WebGLRenderer, before traversal. InstancedMesh bounds follow Three's
     // own aggregate bounds; never test an individual instance independently.
+    let transmission = false;
     scene.traverseVisible(object => {
+      // Refraction uses an auxiliary full-view background pass. An off-scissor
+      // object can still contribute through glass: keep its complete scene.
+      if (object.isMesh) {
+        if (Array.isArray(object.material)) {
+          for (const material of object.material) if (material?.transmission > 0) transmission = true;
+        } else if (object.material?.transmission > 0) transmission = true;
+      }
       if (!object.isMesh || !object.frustumCulled || object.isSkinnedMesh
         || object.children.length) return;
       for (const key in object.geometry?.morphAttributes) if (object.geometry.morphAttributes[key]?.length) return;
@@ -56,9 +64,10 @@ export class LabPortalRenderCulling {
       this.tested++;
       if (!this.frustum.intersectsObject(object)) {
         this.hidden.push(object);
-        object.visible = false;
       }
     });
+    if (transmission) { this.hidden.length = 0; this.active = false; return; }
+    for (const object of this.hidden) object.visible = false;
   }
 
   end() {
diff --git a/tests/lab-portal-performance.test.js b/tests/lab-portal-performance.test.js
index cae3b0a..1cd7341 100644
--- a/tests/lab-portal-performance.test.js
+++ b/tests/lab-portal-performance.test.js
@@ -175,3 +175,16 @@ test('replacing an exit retires the old logical scene root even though GPU resou
   rig.updatePortalClipping();assert.equal(rig.portalExit,null);assert.equal(rig.mainClippingPlanes.length,0);
  }finally{p.dispose();}
 });
+
+
+test('refraction and scene override preserve the full background submission path',()=>{
+ const f=viewFixture(),side=f.add(5,0,-10),glass=f.add(0,0,-8);
+ glass.material.dispose();glass.material=new THREE.MeshPhysicalMaterial({transmission:.5});
+ try{
+  f.start();assert.equal(side.visible,true);assert.equal(f.culling.hidden.length,0);f.culling.end();
+  glass.material.transmission=0;f.scene.overrideMaterial=new THREE.MeshBasicMaterial();
+  f.start();assert.equal(side.visible,true);assert.equal(f.culling.hidden.length,0);f.culling.end();
+  f.scene.overrideMaterial.dispose();f.scene.overrideMaterial=null;
+  f.start();assert.equal(side.visible,false);
+ }finally{f.dispose();}
+});
