# P07V01 — airborne shot into the raised cassette

Base: `16f9755c95540e1c58238d7b5d0d628c348e88e1`. One critical design correction before P02 visual load feedback. This pass does not redesign the room, claim final acceptance, or close issue #32.

## Why this takes priority

The supplied 22.219 s player clip shows a shot toward the raised cassette while falling, a walking return, and arrival at the upper exit. The recording does not show a clean restart or all preceding cargo actions. Do not report its length as first-solution time or infer a false victory from the clip alone.

A related complete route was reproduced from a clean reset using ordinary input: prepare the floor inlet, run and jump diagonally from the departure edge, shoot the raised ceramic, return by the permanent stairs, collect the same companion, and fall through the prepared pair. Three on-face aim variants reached a real joint win with the brake still engaged and the receiver unused. No actor/portal/actuator positions or win flags were assigned by the route. It is a legitimate use of the old geometry, not player misuse or a physics cheat.

## Small correction

Only the existing upper side return of the cassette casing changes. Its rear edge and height stay fixed; the front end continues to x=-2.05 beside the permanent service pocket. The visible mesh and its ordinary collider have identical bounds. This screens the diagonal side view without adding a state-dependent firing veto, forbidden white face, invisible wall, or mandatory-stage win condition. Both output apertures, moving face, low/high stops and normal trajectories remain unchanged.

The first narrower return still allowed two of the nine timing/aim attempts. It was rejected, not reported as a fix. The retained candidate must pass the unchanged nine-case acceptance matrix plus normal room routes.

## Acceptance evidence

- `node scripts/room21-air-shot-audit.mjs`: three complete input attempts. Set `EXPECT_BLOCKED=0` only on the immutable old source to require actual shortcut wins; the candidate requires physical obstruction and walking recovery.
- `node --test tests/lab-room21-air-shot.test.js`: nine aim/timing attempts and visible-mesh/collider agreement. These are finite samples, not a proof against every possible shortcut.
- `node --test tests/lab-room21.test.js`: retained intended routes, independent preparation orders, recovery, offsets, cargo identity and shared-arrival contract.
- `Room21 airborne-shot regression`: before/after real WebGL from the built production packages. The review-only module is copied after the packaging check and is not part of the published package. The shipped driver executes each complete attempt. The video is a silent 100-frame / 1280x720 / 30 encoded fps airborne-shot excerpt, NOT a full playthrough or a hardware benchmark.
- Normal full PR/deployment gates remain required. Neither a workflow definition nor a running job is a successful check.

Local Chromium denied localhost navigation with `ERR_BLOCKED_BY_ADMINISTRATOR`; no browser policy was changed. Browser evidence is obtained through the existing authorized Actions runner.

## Protected scope and next step

Rooms 1–20, original GLBs, shared collision and portal code, velocities, carry rules, camera, saves and dependencies are unchanged. No raw user desktop recording is published in the repository. Full video acceptance, camera polish, further alternatives and human difficulty remain open.

After this correction is verified, return to P02: one visible connection between the receiver and the cassette, with a load/release clip. P01 remains its separate PR; do not silently claim it is included in this branch.
