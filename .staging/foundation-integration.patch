diff --git a/.github/workflows/deploy-pages.yml b/.github/workflows/deploy-pages.yml
index 0e4cf8d..83d0b5a 100644
--- a/.github/workflows/deploy-pages.yml
+++ b/.github/workflows/deploy-pages.yml
@@ -88,0 +89,9 @@ jobs:
+      - name: Verify the new default campaign on the published site
+        env:
+          PAGE_URL: ${{ steps.deployment.outputs.page_url }}
+          BUILD_COMMIT: ${{ github.sha }}
+          RECORD: '0'
+          ONLY_NORMAL: '1'
+        run: |
+          export CHROME_PATH="$(command -v google-chrome || command -v chromium)"
+          node scripts/foundation-browser.mjs
@@ -97,0 +107 @@ jobs:
+            qa/foundation-browser/
diff --git a/.github/workflows/verified-build.yml b/.github/workflows/verified-build.yml
index c1890eb..57584f4 100644
--- a/.github/workflows/verified-build.yml
+++ b/.github/workflows/verified-build.yml
@@ -314,0 +315,39 @@ jobs:
+
+  foundation:
+    name: New first chapter room ${{ matrix.room }} and exploratory return
+    runs-on: ubuntu-latest
+    timeout-minutes: 25
+    strategy:
+      fail-fast: false
+      matrix:
+        room: [1, 2, 3, 4, 5]
+    steps:
+      - uses: actions/checkout@v4
+        with:
+          ref: ${{ inputs.revision }}
+      - uses: actions/setup-node@v4
+        with:
+          node-version: 22
+          cache: npm
+      - run: |
+          npm ci
+          npm install --no-save --package-lock=false puppeteer-core@24.16.0
+          npm run build
+          node scripts/stamp-build.mjs v40-laboratory-33
+      - name: Default public entry, ordinary and alternative inputs, saved progress and next button
+        env:
+          ROOMS: ${{ matrix.room }}
+          RECORD: ${{ inputs.record_puzzle && '1' || '0' }}
+        run: |
+          npm run preview -- --host 127.0.0.1 --port 4173 >foundation-preview.log 2>&1 &
+          for i in {1..30}; do curl -fsS http://127.0.0.1:4173/ >/dev/null && break; sleep 1; done
+          export CHROME_PATH="$(command -v google-chrome || command -v chromium)"
+          node scripts/foundation-browser.mjs
+      - uses: actions/upload-artifact@v4
+        if: always()
+        with:
+          name: foundation-chapter-${{ matrix.room }}
+          path: |
+            qa/foundation-browser/
+            foundation-preview.log
+          retention-days: 14
diff --git a/index.html b/index.html
index cc3af21..998569e 100644
--- a/index.html
+++ b/index.html
@@ -25 +25 @@
-        <div class="hero-meta"><span id="campaign-count">30 уровней</span><span>Исследуй пространство</span><span>Действуй в нужный момент</span></div>
+        <div class="hero-meta"><span id="campaign-count">Новая кампания · первые 5 испытаний</span><span>Исследуй пространство</span><span>Действуй в нужный момент</span></div>
diff --git a/scripts/flight-audio-browser.mjs b/scripts/flight-audio-browser.mjs
index 47bdad8..19c0df0 100644
--- a/scripts/flight-audio-browser.mjs
+++ b/scripts/flight-audio-browser.mjs
@@ -19 +19 @@ export async function runFlightAudioBrowser({browser,baseUrl='http://127.0.0.1:4
-    const url=new URL(baseUrl);url.searchParams.set('debug','1');url.searchParams.set('level','1');
+    const url=new URL(baseUrl);url.searchParams.set('edition','classic');url.searchParams.set('debug','1');url.searchParams.set('level','1');
diff --git a/scripts/portal-shot-browser.mjs b/scripts/portal-shot-browser.mjs
index f1a7878..5026cd4 100644
--- a/scripts/portal-shot-browser.mjs
+++ b/scripts/portal-shot-browser.mjs
@@ -152,0 +153 @@ export async function runPortalShotBrowser({ browser, baseUrl = 'http://127.0.0.
+    url.searchParams.set('edition', 'classic');
diff --git a/scripts/stamp-build.mjs b/scripts/stamp-build.mjs
index 7d05501..2e9015d 100644
--- a/scripts/stamp-build.mjs
+++ b/scripts/stamp-build.mjs
@@ -17 +17 @@ const info={commit,version,artVersion:'v40-research-laboratory',levels:CAMPAIGN.
- features:{campaignRooms:33,campaignFinaleLevel:33,separateVelocityMode:false,openChamberReview:{version:'research-laboratory-v3',query:'edition=open',rooms:[24,28,30,31,32,33],separateSave:true}},
+ features:{defaultEdition:'foundation',foundation:{version:'foundation-v1',rooms:[1,2,3,4,5],separateSave:true},archiveQuery:'edition=classic',campaignRooms:33,campaignFinaleLevel:33,separateVelocityMode:false,openChamberReview:{version:'research-laboratory-v3',query:'edition=open',rooms:[24,28,30,31,32,33],separateSave:true}},
diff --git a/scripts/v8-browser.mjs b/scripts/v8-browser.mjs
index dc66899..98ec32a 100644
--- a/scripts/v8-browser.mjs
+++ b/scripts/v8-browser.mjs
@@ -52 +52 @@ assert.equal(expectedFiles.length,expectedIds.length,'Every selected course depe
-function startUrl(level){const url=new URL(root);url.searchParams.set('debug','1');url.searchParams.set('level',String(level));return url.href;}
+function startUrl(level){const url=new URL(root);url.searchParams.set('edition','classic');url.searchParams.set('debug','1');url.searchParams.set('level',String(level));return url.href;}
diff --git a/scripts/v8-yandex-browser.mjs b/scripts/v8-yandex-browser.mjs
index f8c0086..3a49b13 100644
--- a/scripts/v8-yandex-browser.mjs
+++ b/scripts/v8-yandex-browser.mjs
@@ -18 +18 @@ try{
- await page.goto('http://127.0.0.1:4174/?debug=1',{waitUntil:'networkidle2'});
+ await page.goto('http://127.0.0.1:4174/?edition=classic&debug=1',{waitUntil:'networkidle2'});
diff --git a/scripts/verify-public.mjs b/scripts/verify-public.mjs
index 8c07213..795ca32 100644
--- a/scripts/verify-public.mjs
+++ b/scripts/verify-public.mjs
@@ -37 +37 @@ try{
-  await page.goto(base+'?debug=1&level='+level+'&revision='+expected,{waitUntil:'networkidle2'});
+  await page.goto(base+'?edition=classic&debug=1&level='+level+'&revision='+expected,{waitUntil:'networkidle2'});
diff --git a/src/game/LabCampaignLevels.js b/src/game/LabCampaignLevels.js
index a527c21..1ab71ce 100644
--- a/src/game/LabCampaignLevels.js
+++ b/src/game/LabCampaignLevels.js
@@ -0,0 +1 @@
+import {FOUNDATION_BUILDERS,FOUNDATION_SPECS} from './LabFoundationChambers.js';
@@ -50,0 +52 @@ export function buildLabCampaignLevel(game,index){
+ if(game.chamberEdition==='foundation'&&FOUNDATION_BUILDERS[index])return FOUNDATION_BUILDERS[index](game,index);
@@ -65 +67 @@ export function buildLabCampaignLevel(game,index){
-export function campaignSpec(game,index){return game.chamberEdition==='open'&&OPEN_SPECS[index]||CAMPAIGN[index];}
+export function campaignSpec(game,index){return game.chamberEdition==='foundation'&&FOUNDATION_SPECS[index]||game.chamberEdition==='open'&&OPEN_SPECS[index]||CAMPAIGN[index];}
diff --git a/src/game/LabTutorial.js b/src/game/LabTutorial.js
index ebbb3ed..a237c42 100644
--- a/src/game/LabTutorial.js
+++ b/src/game/LabTutorial.js
@@ -10 +10 @@ export class LabTutorial {
-    if(contextual)return g.heldCube?['room-put-down','E','Поставить друга. Для работы с пультом нужны свободные руки.',false]:contextual;
+    if(contextual)return g.heldCube&&!l.contextHandlesCarry?['room-put-down','E','Поставить друга. Для работы с пультом нужны свободные руки.',false]:contextual;
diff --git a/src/game/LabV8Journey.js b/src/game/LabV8Journey.js
index 4c6a65e..3386c6b 100644
--- a/src/game/LabV8Journey.js
+++ b/src/game/LabV8Journey.js
@@ -86 +86,4 @@ export async function runV8Journey(game,{onMilestone=()=>{},scenario=null,journe
-    if(level.researchChamber){
+    if(level.foundationChamber){
+      const {runFoundationJourney}=await import('./LabFoundationJourney.js');
+      await runFoundationJourney({game,level,walk,wait,aim,look,until,pickup,enter,mark,frame,worldMove,stop},journeyOptions);
+    }else if(level.researchChamber){
diff --git a/src/main.js b/src/main.js
index b48561e..495d4c9 100644
--- a/src/main.js
+++ b/src/main.js
@@ -6,0 +7 @@ import {readCampaignRoute,nextCampaignLevel} from './game/LabCampaignRoute.js';
+import {FOUNDATION_INDICES,readFoundationEdition,foundationStorage,nextFoundationLevel} from './game/LabFoundationEdition.js';
@@ -10,2 +11,2 @@ const debug=query.get('debug')==='1'||query.get('smoke')==='1';
-const campaignRoute=readCampaignRoute(query,CAMPAIGN.length),openEdition=readOpenEdition(query);
-const availableRooms=openEdition.enabled?OPEN_ROOM_INDICES:CAMPAIGN.map((_,i)=>i);
+const campaignRoute=readCampaignRoute(query,CAMPAIGN.length),openEdition=readOpenEdition(query),foundationEdition=readFoundationEdition(query);
+const availableRooms=foundationEdition.enabled?FOUNDATION_INDICES:openEdition.enabled?OPEN_ROOM_INDICES:CAMPAIGN.map((_,i)=>i);
@@ -16 +17 @@ let storage;try{storage=localStorage;}catch{}
-const preferences=new LabPreferences(openEdition.enabled?openEditionStorage(storage):storage),holds=new Set();
+const preferences=new LabPreferences(foundationEdition.enabled?foundationStorage(storage):openEdition.enabled?openEditionStorage(storage):storage),holds=new Set();
@@ -38 +39 @@ function showVictory(){
-  const last=game.levelIndex===CAMPAIGN.length-1;
+  const last=game.levelIndex===availableRooms.at(-1);
@@ -42 +43 @@ function showVictory(){
-  $('#win-screen .muted').textContent=last?(openEdition.enabled?'Пройдены все испытания этой версии.':'Все доступные испытания завершены. Друг добрался вместе с тобой.'):'Получилось! Следующее испытание добавит новую идею.';
+  $('#win-screen .muted').textContent=last?(foundationEdition.enabled?'Первая глава завершена. В меню отдельно доступны прежние испытания.':openEdition.enabled?'Пройдены все испытания этой версии.':'Все доступные испытания завершены. Друг добрался вместе с тобой.'):'Получилось! Следующее испытание добавит новую идею.';
@@ -97 +98 @@ game.epicMode=false;
-game.chamberEdition=openEdition.enabled?'open':'classic';
+game.chamberEdition=foundationEdition.enabled?'foundation':openEdition.enabled?'open':'classic';
@@ -101 +102 @@ game.quality={...QUALITY_PRESETS[preferences.value.quality]};game.tutorial.enabl
-game.levelIndex=openEdition.enabled?openEdition.levelIndex:campaignRoute.levelIndex;
+game.levelIndex=foundationEdition.enabled?foundationEdition.levelIndex:openEdition.enabled?openEdition.levelIndex:campaignRoute.levelIndex;
@@ -103,2 +104,8 @@ choices();$('#level-select').value=String(game.levelIndex);
-$('#campaign-count').textContent=openEdition.enabled?'3 пересобранных уровня · отдельная версия':`${CAMPAIGN.length} уровней`;
-if(openEdition.enabled){$('#start-screen .brand').textContent='ОТКРЫТЫЕ ПРОСТРАНСТВА · ПЕРЕСБОРКА';$('#start-screen .lead').textContent='Три новые планировки: подвесной причал, гидравлические площади и полёт за энергоблок. Прогресс старой кампании хранится отдельно.';}
+$('#campaign-count').textContent=foundationEdition.enabled?'Новая кампания · первые 5 испытаний':openEdition.enabled?`${OPEN_ROOM_INDICES.length} лабораторных испытаний · отдельная версия`:`Архив · ${CAMPAIGN.length} испытания`;
+if(foundationEdition.enabled){$('#start-screen .brand').textContent='НОВАЯ КАМПАНИЯ · ОТ ОТКРЫТИЯ К ЭКСПЕРИМЕНТУ';$('#start-screen .lead').textContent='Начни с простой связи. Узнай, что умеют свет, движение и высота. Здесь можно пробовать, ошибаться и возвращаться за другом. Каждая комната добавляет новую идею.';}
+else if(openEdition.enabled){$('#start-screen .brand').textContent='ЛАБОРАТОРНЫЕ ИСПЫТАНИЯ';$('#start-screen .lead').textContent='Камеры 24, 28, 30 и 31–33. Эта подборка и новая первая глава хранят прогресс отдельно от архива.';}
+const editionNav=document.createElement('nav');editionNav.className='edition-navigation';editionNav.setAttribute('aria-label','Версии кампании');
+for(const [id,text,href]of [['foundation','Новая кампания · с начала','?edition=foundation&level=1'],['open','Лабораторная глава 31–33','?edition=open&level=31'],['classic','Архив · 33 испытания','?edition=classic&level=1']]){
+ if(game.chamberEdition===id)continue;const a=document.createElement('a');a.textContent=text;a.href=href;editionNav.append(a);
+}
+$('#start-screen .hero-footer').before(editionNav);
@@ -123 +130 @@ $('#play-button').addEventListener('click',()=>enterLevel(Number($('#level-selec
-$('#play-again-button').addEventListener('click',()=>enterLevel(openEdition.enabled?nextOpenRoom(game.levelIndex):nextCampaignLevel(game.levelIndex,CAMPAIGN.length)));
+$('#play-again-button').addEventListener('click',()=>enterLevel(foundationEdition.enabled?nextFoundationLevel(game.levelIndex):openEdition.enabled?nextOpenRoom(game.levelIndex):nextCampaignLevel(game.levelIndex,CAMPAIGN.length)));
diff --git a/src/styles.css b/src/styles.css
index b90c92b..d7e1ace 100644
--- a/src/styles.css
+++ b/src/styles.css
@@ -201,0 +202,2 @@ body[data-external-pause="true"] .play-tools,body[data-external-pause="true"] .l
+
+.edition-navigation{display:flex;flex-wrap:wrap;gap:.5rem 1.2rem;margin:1.1rem 0;font-size:.82rem}.edition-navigation a{color:var(--muted,#bfd5d7);text-underline-offset:.25em}.edition-navigation a:hover,.edition-navigation a:focus-visible{color:#fff}
